# Silo Lists — optional Supabase sync (free tier is enough for two people)
# Copy to .env.local for local dev. Omit both to use local-only mode on this device.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
